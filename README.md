# live data branch

Machine-written. `live.json` is published here by `ed_bridge.py` on
Wayne's PC and fetched client-side by the site.

This branch is intentionally an orphan with a **single commit** -
the publisher amends and force-pushes, so frequent updates never
grow history. Do not merge it into `main`, and do not expect its
history to be meaningful.
